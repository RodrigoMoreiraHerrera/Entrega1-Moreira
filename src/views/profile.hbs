
<div class="max-w-2xl mx-auto mt-10 bg-white rounded-lg shadow-md p-8">
    <h1 class="text-3xl font-bold mb-6 text-center text-gray-800">Perfil de Usuario</h1>
    <div class="space-y-4">
        <div>
            <label class="block text-gray-600 font-semibold">Nombre:</label>
            <div class="bg-gray-100 rounded px-4 py-2 text-gray-800">{{user.first_name}}</div>
        </div>
        <div>
            <label class="block text-gray-600 font-semibold">Apellido:</label>
            <div class="bg-gray-100 rounded px-4 py-2 text-gray-800">{{user.last_name}}</div>
        </div>
        <div>
            <label class="block text-gray-600 font-semibold">Email:</label>
            <div class="bg-gray-100 rounded px-4 py-2 text-gray-800">{{user.email}}</div>
        </div>
        <div>
            <label class="block text-gray-600 font-semibold">Edad:</label>
            <div class="bg-gray-100 rounded px-4 py-2 text-gray-800">{{user.age}}</div>
        </div>
        <div>
            <label class="block text-gray-600 font-semibold">Rol:</label>
            <div class="bg-gray-100 rounded px-4 py-2 text-gray-800">{{user.role}}</div>
        </div>
    </div>
</div>